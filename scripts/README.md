# Scripts

Utility scripts for broader hunt operations.

| Script | Purpose |
|:---|:---|
| `full_hunt.sh` | End-to-end hunt pipeline orchestrator — runs recon + vuln scan + validation in sequence |
| `dork_runner.py` | Google dork automation for passive recon on a target |
